[project]
name = "livekit-voice-agent"
version = "0.1.0"
requires-python = ">=3.10,<3.14"
dependencies = [
    "livekit-agents[rime,silero,turn-detector]~=1.3",
    "livekit-plugins-noise-cancellation~=0.2",
    "python-dotenv",
]
